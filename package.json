{
  "name": "image-generator-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Two-image upload tool that proxies to an n8n webhook and renders the generated image.",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  }
}
